// Patch for sign_in_with_apple namespace issue
afterEvaluate {
    project(':sign_in_with_apple').android {
        if (!namespace) {
            namespace = 'com.aboutyou.dart_packages.sign_in_with_apple'
        }
    }
}
